---
jupyter:
  jupytext:
    text_representation:
      extension: .md
      format_name: markdown
      format_version: '1.3'
  kernelspec:
    display_name: Python 3
    language: python
    name: python3
---

# 레슨 05 — 세션과 쿠키 상태 관리

[![Open in Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/Kevin-innovation/jupyter-lecture/blob/main/python-web-automation/lectures/05/[학생용] 레슨 05 — 세션과 쿠키 상태 관리.ipynb)

이 노트북은 읽기와 따라하기용 강의 노트북이다. 학생은 셀을 위에서 아래로 실행하며 웹 자동화에서 상태가 어떻게 유지되고 화면 조작이 어떤 순서로 기록되는지 확인한다. 세션과 쿠키 상태 관리는 실제 사이트 대신 합성 fixture로 안전하게 연습한다.

## 학습 목표

1. requests.Session의 역할을 설명한다.
2. 쿠키가 필터와 로그인 상태를 어떻게 유지하는지 확인한다.
3. 합성 로그인 폼에서 hidden token과 input name을 읽는다.
4. 장바구니 상태를 세션 객체로 관리한다.
5. 세션 이벤트 로그를 CSV로 저장한다.

---

## 1. 세션이 필요한 이유

HTTP 요청은 기본적으로 서로 독립적이다. 세션은 쿠키와 공통 헤더를 유지해 다음 요청에서도 같은 사용자 상태를 이어간다.

~~~python
import os
import re
import time
import csv
from pathlib import Path
from urllib.parse import urljoin, urlparse, parse_qs, urlencode

try:
    import requests
    from bs4 import BeautifulSoup
except ImportError:
    import sys, subprocess
    subprocess.check_call([sys.executable, '-m', 'pip', '-q', 'install', 'requests', 'beautifulsoup4'])
    import requests
    from bs4 import BeautifulSoup

IS_COLAB = 'COLAB_GPU' in os.environ or 'COLAB_TPU_ADDR' in os.environ
if IS_COLAB:
    DATA_BASE = 'https://raw.githubusercontent.com/Kevin-innovation/jupyter-lecture/main/python-web-automation/lectures/05/data'
else:
    DATA_BASE = './data'

def load_text(filename):
    if DATA_BASE.startswith('http'):
        url = f'{DATA_BASE}/{filename}'
        response = requests.get(url, timeout=10, headers={'User-Agent': 'D-Lab-Lesson/1.0'})
        response.raise_for_status()
        response.encoding = response.encoding or 'utf-8'
        return response.text
    return Path(DATA_BASE, filename).read_text(encoding='utf-8')

def clean_int(text):
    return int(re.sub(r'[^0-9]', '', str(text)))

class DemoSession:
    def __init__(self):
        self.client = requests.Session()
        self.cart = []
        self.log = []
    def login(self, username, token):
        if token != 'safe-token-05':
            raise ValueError('invalid token')
        self.client.cookies.set('demo_user', username)
        self.client.cookies.set('session_id', 'demo-session-05')
        self.log.append({'event': 'login', 'user': username})
    def set_filter(self, name, value):
        self.client.cookies.set(f'filter_{name}', str(value))
        self.log.append({'event': 'filter', 'name': name, 'value': str(value)})
    def add_to_cart(self, product_id, quantity=1):
        self.cart.append({'product_id': product_id, 'quantity': int(quantity)})
        self.log.append({'event': 'add', 'product_id': product_id, 'quantity': int(quantity)})
    def is_logged_in(self):
        return self.client.cookies.get('session_id') is not None
    def cookies(self):
        return self.client.cookies.get_dict()
    def cart_count(self):
        return sum(item['quantity'] for item in self.cart)

print('colab:', IS_COLAB)
print('data base:', DATA_BASE)

form_html = load_text('login_form.html')
soup = BeautifulSoup(form_html, 'html.parser')
token = soup.select_one('input[name="csrf_token"]')['value']
session = DemoSession()
session.login('student01', token)
print(session.cookies())
~~~

---

## 2. 쿠키로 필터 상태 유지

카테고리 필터 같은 화면 상태는 쿠키나 query string에 남는 경우가 많다.

~~~python
catalog = BeautifulSoup(load_text('catalog_page.html'), 'html.parser')
cards = catalog.select('article.product-card')
session.set_filter('category', 'book')
selected = [card for card in cards if card['data-category'] == session.cookies()['filter_category']]
print(len(selected))
~~~

---

## 3. 장바구니 상태

장바구니는 세션 상태를 설명하기 좋은 예시다. 어떤 상품을 몇 개 담았는지 요청 사이에 유지되어야 한다.

~~~python
session.add_to_cart(selected[0]['data-product-id'], quantity=2)
print(session.cart)
print(session.cart_count())
~~~

---

## 데이터 출처와 안전 규칙

이 레슨의 파일은 모두 수업용 합성 데이터다. 실제 사이트의 개인정보, 로그인 정보, 유료 콘텐츠를 포함하지 않는다. 실제 사이트로 확장할 때는 robots.txt, 이용 약관, 요청 간격, 개인정보 여부를 먼저 확인한다. 수업 중에는 fixture를 반복 실행하며 구조를 익히고, 외부 사이트를 빠르게 반복 요청하지 않는다.

---

## 강의 보강 노트

이 절은 수업 중 교사가 질문으로 풀어낼 수 있는 운영형 설명이다. 학생이 셀을 실행한 뒤 결과만 맞히지 않고 자동화 절차를 말로 설명하도록 돕는다.

### 1. 세션의 의미

세션은 단순한 요청 묶음이 아니라 이전 요청의 상태를 다음 요청에 이어 주는 컨테이너다. 학생에게 쿠키가 어디에 저장되고 어떤 요청에서 다시 쓰이는지 그림처럼 설명하게 하면 장바구니나 필터 흐름을 이해하기 쉽다.

### 2. 쿠키와 개인정보

쿠키 실습은 로그인 우회나 민감정보 수집과 혼동되기 쉽다. 이 단원에서는 실제 계정 없이 합성 HTML과 DemoSession만 사용하며, 개인정보나 인증 토큰을 저장하지 않는다는 원칙을 분명히 한다.

### 3. 상태 변화 관찰

필터를 누르거나 장바구니에 담는 동작은 화면 결과보다 내부 상태가 어떻게 바뀌었는지 보는 것이 중요하다. 학생 답안에는 상태를 출력하는 작은 로그가 있어야 피드백하기 좋다.

### 4. 요청 순서

세션 코드는 순서가 바뀌면 결과가 달라진다. 로그인 폼 읽기, 토큰 확인, 세션 생성, 필터 적용, 결과 확인 순서를 명확히 나누면 오류가 난 위치를 바로 찾을 수 있다.

### 5. 만료와 초기화

실제 서비스에서는 세션이 만료되거나 쿠키 정책이 바뀐다. 수업에서는 만료 구현까지 깊게 들어가지 않더라도, 세션을 새로 만들었을 때 결과가 초기화되는 현상을 확인시킨다.

### 6. 저장하지 말아야 할 것

운영 메모에는 어떤 파일을 만들었는지 남기되 쿠키 원문이나 토큰 값을 제출하게 하지 않는다. 학생이 자동화 결과와 민감 상태를 구분하는 습관을 갖는 것이 이 단원의 핵심이다.

### 7. 디버깅 포인트

세션 결과가 예상과 다르면 먼저 쿠키 key, 선택한 상품 id, 필터 category, 이벤트 로그 순서를 확인한다. print를 많이 넣는 것보다 점검 지점을 정해두는 방식이 낫다.

### 체크포인트 1: 실행 순서 정리한다

학생이 막힌 지점을 찾기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 실행 순서을/를 정리한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 2: 운영 메모 설명한다

운영자가 결과를 이해할 수 있게, 레슨 05 세션과 쿠키 상태 관리에서는 운영 메모을/를 설명한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 3: 출력 형태 저장한다

재실행했을 때 같은 결과를 얻기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 출력 형태을/를 저장한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 4: 반복 단위 되돌아본다

다음 셀에서 재사용하기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 반복 단위을/를 되돌아본다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 5: 상태 값 표준화한다

실제 사이트 확장 전에 위험을 낮추기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 상태 값을/를 표준화한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 6: 저장 경로 검증한다

저장 파일의 신뢰도를 높이기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 저장 경로을/를 검증한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 7: 실행 순서 확인한다

코랩과 로컬 실행 차이를 줄이기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 실행 순서을/를 확인한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 8: 운영 메모 분리한다

수업 중 피드백 시간을 줄이기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 운영 메모을/를 분리한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 9: 출력 형태 기록한다

학생이 막힌 지점을 찾기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 출력 형태을/를 기록한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 10: 반복 단위 비교한다

운영자가 결과를 이해할 수 있게, 레슨 05 세션과 쿠키 상태 관리에서는 반복 단위을/를 비교한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 11: 상태 값 요약한다

재실행했을 때 같은 결과를 얻기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 상태 값을/를 요약한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 12: 저장 경로 검토한다

다음 셀에서 재사용하기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 저장 경로을/를 검토한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 13: 실행 순서 정리한다

실제 사이트 확장 전에 위험을 낮추기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 실행 순서을/를 정리한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 14: 운영 메모 설명한다

저장 파일의 신뢰도를 높이기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 운영 메모을/를 설명한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 15: 출력 형태 저장한다

코랩과 로컬 실행 차이를 줄이기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 출력 형태을/를 저장한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 16: 반복 단위 되돌아본다

수업 중 피드백 시간을 줄이기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 반복 단위을/를 되돌아본다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 17: 상태 값 표준화한다

학생이 막힌 지점을 찾기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 상태 값을/를 표준화한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 18: 저장 경로 검증한다

운영자가 결과를 이해할 수 있게, 레슨 05 세션과 쿠키 상태 관리에서는 저장 경로을/를 검증한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 19: 실행 순서 확인한다

재실행했을 때 같은 결과를 얻기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 실행 순서을/를 확인한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 20: 운영 메모 분리한다

다음 셀에서 재사용하기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 운영 메모을/를 분리한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 21: 출력 형태 기록한다

실제 사이트 확장 전에 위험을 낮추기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 출력 형태을/를 기록한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 22: 반복 단위 비교한다

저장 파일의 신뢰도를 높이기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 반복 단위을/를 비교한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 23: 상태 값 요약한다

코랩과 로컬 실행 차이를 줄이기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 상태 값을/를 요약한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 24: 저장 경로 검토한다

수업 중 피드백 시간을 줄이기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 저장 경로을/를 검토한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 25: 실행 순서 정리한다

학생이 막힌 지점을 찾기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 실행 순서을/를 정리한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 26: 운영 메모 설명한다

운영자가 결과를 이해할 수 있게, 레슨 05 세션과 쿠키 상태 관리에서는 운영 메모을/를 설명한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 27: 출력 형태 저장한다

재실행했을 때 같은 결과를 얻기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 출력 형태을/를 저장한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 28: 반복 단위 되돌아본다

다음 셀에서 재사용하기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 반복 단위을/를 되돌아본다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 29: 상태 값 표준화한다

실제 사이트 확장 전에 위험을 낮추기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 상태 값을/를 표준화한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 30: 저장 경로 검증한다

저장 파일의 신뢰도를 높이기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 저장 경로을/를 검증한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 31: 실행 순서 확인한다

코랩과 로컬 실행 차이를 줄이기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 실행 순서을/를 확인한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 32: 운영 메모 분리한다

수업 중 피드백 시간을 줄이기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 운영 메모을/를 분리한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 33: 출력 형태 기록한다

학생이 막힌 지점을 찾기 위해, 레슨 05 세션과 쿠키 상태 관리에서는 출력 형태을/를 기록한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.
