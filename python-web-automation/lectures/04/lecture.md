---
jupyter:
  jupytext:
    text_representation:
      extension: .md
      format_name: markdown
      format_version: '1.3'
  kernelspec:
    display_name: Python 3
    language: python
    name: python3
---

# 레슨 04 — 파일 다운로드와 폴더 정리

이 노트북은 읽기와 따라하기용 강의 노트북이다. 학생은 셀을 위에서 아래로 실행하며 입력 데이터가 어떤 구조로 바뀌는지 확인한다. 파일 다운로드와 폴더 정리는 실제 업무 자동화에서 자주 등장하는 반복 패턴을 합성 fixture로 안전하게 연습한다.

## 학습 목표

1. HTML에서 다운로드 링크와 파일 메타데이터를 추출한다.
2. bytes 데이터를 파일로 저장한다.
3. 파일명을 안전하게 정리한다.
4. manifest 기준으로 폴더를 나누어 저장한다.
5. 다운로드 로그 CSV를 만든다.

---

## 1. 파일 링크 구조

자료실 HTML의 a.file-link는 href와 data-type, data-week 속성을 가진다.

~~~python
import os
import re
import time
import csv
from pathlib import Path
from urllib.parse import urljoin, urlparse, parse_qs, urlencode

try:
    import requests
    from bs4 import BeautifulSoup
except ImportError:
    import sys, subprocess
    subprocess.check_call([sys.executable, '-m', 'pip', '-q', 'install', 'requests', 'beautifulsoup4'])
    import requests
    from bs4 import BeautifulSoup

IS_COLAB = 'COLAB_GPU' in os.environ or 'COLAB_TPU_ADDR' in os.environ
if IS_COLAB:
    DATA_BASE = 'https://raw.githubusercontent.com/Kevin-innovation/jupyter-lecture/main/python-web-automation/lectures/04/data'
else:
    DATA_BASE = './data'

def load_text(filename):
    if DATA_BASE.startswith('http'):
        url = f'{DATA_BASE}/{filename}'
        response = requests.get(url, timeout=10, headers={'User-Agent': 'D-Lab-Lesson/1.0'})
        response.raise_for_status()
        response.encoding = response.encoding or 'utf-8'
        return response.text
    return Path(DATA_BASE, filename).read_text(encoding='utf-8')

def load_bytes(filename):
    if DATA_BASE.startswith('http'):
        url = f'{DATA_BASE}/{filename}'
        response = requests.get(url, timeout=10, headers={'User-Agent': 'D-Lab-Lesson/1.0'})
        response.raise_for_status()
        return response.content
    return Path(DATA_BASE, filename).read_bytes()

def clean_int(text):
    return int(re.sub(r'[^0-9]', '', str(text)))

def safe_filename(name):
    return re.sub(r'[^A-Za-z0-9_.-]+', '_', str(name)).strip('_')

def save_bytes(relative_path, target_dir):
    target_dir = Path(target_dir)
    target_dir.mkdir(parents=True, exist_ok=True)
    filename = safe_filename(Path(relative_path).name)
    target = target_dir / filename
    target.write_bytes(load_bytes(relative_path))
    return target

print('colab:', IS_COLAB)
print('data base:', DATA_BASE)

html_text = load_text('resource_center.html')
soup = BeautifulSoup(html_text, 'html.parser')
links = soup.select('a.file-link')
print(len(links), links[0]['href'])
~~~

---

## 2. bytes 저장

다운로드 파일은 텍스트가 아닐 수 있으므로 bytes로 읽고 write_bytes로 저장한다.

~~~python
content = load_bytes('files/lesson_plan.md')
print(type(content), len(content))
~~~

---

## 3. manifest 기반 폴더 정리

manifest의 group, type, path 컬럼을 사용해 저장 위치와 로그를 만든다.

~~~python
manifest = list(csv.DictReader(load_text('manifest.csv').splitlines()))
for row in manifest[:3]:
    print(row['file_name'], row['group'], row['type'])
~~~

---

## 데이터 출처와 안전 규칙

이 레슨의 파일은 모두 수업용 합성 데이터다. 실제 사이트의 개인정보, 로그인 정보, 유료 콘텐츠를 포함하지 않는다. 실제 사이트로 확장할 때는 robots.txt, 이용 약관, 요청 간격, 수집 목적을 먼저 확인한다. 수업 중에는 fixture를 반복 실행하며 구조를 익히고, 외부 사이트를 빠르게 반복 요청하지 않는다.

---

## 강의 보강 노트

이 절은 수업 중 교사가 질문으로 풀어낼 수 있는 운영형 설명이다. 학생이 셀을 실행한 뒤 결과만 맞히지 않고 자동화 절차를 말로 설명하도록 돕는다.

### 1. 다운로드 대상 식별

자료실 자동화는 링크를 모두 받는 것이 아니라 받을 파일과 건너뛸 파일을 구분하는 작업이다. 확장자, 파일 크기, 카테고리, 공개 상태를 기준으로 선택하게 하면 무분별한 다운로드를 막을 수 있다.

### 2. 폴더 구조 설계

저장 폴더를 먼저 정하지 않으면 결과 파일이 코랩 작업 폴더에 뒤섞인다. 날짜, 과정명, 파일 유형을 기준으로 폴더를 나누는 예시는 이후 운영 자동화에서도 바로 쓰인다.

### 3. 파일명 정리

웹에서 받은 파일명에는 공백, 괄호, 한글, 특수문자가 섞일 수 있다. 학생에게 원본 파일명을 보존하되 저장용 이름은 안전하게 정리하는 이유를 설명하게 한다.

### 4. 중복 다운로드 방지

같은 파일을 여러 번 받지 않으려면 URL, 파일명, 크기 중 어떤 값을 key로 삼을지 정해야 한다. fixture에서는 manifest를 기준으로 중복 판단을 연습하고, 실제 서비스에서는 수정일도 함께 고려한다.

### 5. 실패 기록

다운로드 실패를 단순히 print로 지나가면 나중에 누락을 찾기 어렵다. 실패한 URL, 이유, 재시도 여부를 별도 로그에 남기는 구조를 기본 답안으로 본다.

### 6. 자료실 안전 규칙

대용량 파일과 비공개 자료가 섞인 실제 사이트에서는 자동 다운로드가 쉽게 문제를 만든다. 수업에서는 합성 파일만 다루고, 실제 적용 전에는 권한과 저장 위치를 반드시 확인한다.

### 7. 검수 방식

다운로드 자동화의 완료 기준은 파일 개수만이 아니다. 파일이 열리는지, 크기가 0이 아닌지, manifest와 실제 저장 목록이 맞는지까지 확인해야 운영에 쓸 수 있다.

### 체크포인트 1: 결과 행 수 검토한다

저장 파일의 신뢰도를 높이기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 결과 행 수을/를 검토한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 2: 검증 기준 정리한다

코랩과 로컬 실행 차이를 줄이기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 검증 기준을/를 정리한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 3: 중간 변수 설명한다

수업 중 피드백 시간을 줄이기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 중간 변수을/를 설명한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 4: 입력 파일 저장한다

학생이 막힌 지점을 찾기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 입력 파일을/를 저장한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 5: 선택자 되돌아본다

운영자가 결과를 이해할 수 있게, 레슨 04 파일 다운로드와 폴더 정리에서는 선택자을/를 되돌아본다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 6: 오류 메시지 표준화한다

재실행했을 때 같은 결과를 얻기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 오류 메시지을/를 표준화한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 7: 결과 행 수 검증한다

다음 셀에서 재사용하기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 결과 행 수을/를 검증한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 8: 검증 기준 확인한다

실제 사이트 확장 전에 위험을 낮추기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 검증 기준을/를 확인한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 9: 중간 변수 분리한다

저장 파일의 신뢰도를 높이기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 중간 변수을/를 분리한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 10: 입력 파일 기록한다

코랩과 로컬 실행 차이를 줄이기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 입력 파일을/를 기록한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 11: 선택자 비교한다

수업 중 피드백 시간을 줄이기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 선택자을/를 비교한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 12: 오류 메시지 요약한다

학생이 막힌 지점을 찾기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 오류 메시지을/를 요약한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 13: 결과 행 수 검토한다

운영자가 결과를 이해할 수 있게, 레슨 04 파일 다운로드와 폴더 정리에서는 결과 행 수을/를 검토한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 14: 검증 기준 정리한다

재실행했을 때 같은 결과를 얻기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 검증 기준을/를 정리한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 15: 중간 변수 설명한다

다음 셀에서 재사용하기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 중간 변수을/를 설명한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 16: 입력 파일 저장한다

실제 사이트 확장 전에 위험을 낮추기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 입력 파일을/를 저장한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 17: 선택자 되돌아본다

저장 파일의 신뢰도를 높이기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 선택자을/를 되돌아본다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 18: 오류 메시지 표준화한다

코랩과 로컬 실행 차이를 줄이기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 오류 메시지을/를 표준화한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 19: 결과 행 수 검증한다

수업 중 피드백 시간을 줄이기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 결과 행 수을/를 검증한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 20: 검증 기준 확인한다

학생이 막힌 지점을 찾기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 검증 기준을/를 확인한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 21: 중간 변수 분리한다

운영자가 결과를 이해할 수 있게, 레슨 04 파일 다운로드와 폴더 정리에서는 중간 변수을/를 분리한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 22: 입력 파일 기록한다

재실행했을 때 같은 결과를 얻기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 입력 파일을/를 기록한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 23: 선택자 비교한다

다음 셀에서 재사용하기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 선택자을/를 비교한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 24: 오류 메시지 요약한다

실제 사이트 확장 전에 위험을 낮추기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 오류 메시지을/를 요약한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 25: 결과 행 수 검토한다

저장 파일의 신뢰도를 높이기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 결과 행 수을/를 검토한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 26: 검증 기준 정리한다

코랩과 로컬 실행 차이를 줄이기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 검증 기준을/를 정리한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 27: 중간 변수 설명한다

수업 중 피드백 시간을 줄이기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 중간 변수을/를 설명한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 28: 입력 파일 저장한다

학생이 막힌 지점을 찾기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 입력 파일을/를 저장한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 29: 선택자 되돌아본다

운영자가 결과를 이해할 수 있게, 레슨 04 파일 다운로드와 폴더 정리에서는 선택자을/를 되돌아본다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 30: 오류 메시지 표준화한다

재실행했을 때 같은 결과를 얻기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 오류 메시지을/를 표준화한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 31: 결과 행 수 검증한다

다음 셀에서 재사용하기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 결과 행 수을/를 검증한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 32: 검증 기준 확인한다

실제 사이트 확장 전에 위험을 낮추기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 검증 기준을/를 확인한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.

### 체크포인트 33: 중간 변수 분리한다

저장 파일의 신뢰도를 높이기 위해, 레슨 04 파일 다운로드와 폴더 정리에서는 중간 변수을/를 분리한다는 과정을 별도의 단계로 둔다. 이때 학생은 화면에 보이는 값 하나보다 어떤 입력에서 어떤 변환을 거쳐 결과가 나왔는지 설명해야 한다. 강사는 변수명, 출력 형태, 저장 여부를 함께 확인하며 다음 실행에서도 같은 결과가 나오는지 묻는다.
